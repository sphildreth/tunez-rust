[package]
name = "filesystem-provider"
version = "0.1.0"
edition = "2021"
license = "MIT"
publish = false

[dependencies]
walkdir = "2"
path-clean = "1"
tunez-core = { path = "../../tunez-core" }

[dev-dependencies]
tempfile = "3"
